# App Bootstrap

The app target is generated from `project.yml` in the repo root.

Recommended initial conventions:

- iOS 18+ deployment target unless a lower target is required later
- SwiftUI app lifecycle
- feature-first folder layout after bootstrap
- no storyboard-based navigation
